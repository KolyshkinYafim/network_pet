export enum Theme {
  dark = "dark",
  light = "light",
}

export enum ButtonType {
  button = "button",
  submit = "submit",
  reset = "reset",
}

export enum ButtonColor {
  default = "default",
  primary = "primary",
  secondary = "secondary",
  success = "success",
  warning = "warning",
  danger = "danger",
  undefined = undefined,
}
